# Approved pricing change

- Pricing algorithms must remain interchangeable behind the existing `PricingStrategy` interface.
- Two current algorithms and their contract behavior must remain compatible.
- Runtime-authored business rules are not required.
- Adding a rules engine is an optional proposal, not an approved dependency requirement.
