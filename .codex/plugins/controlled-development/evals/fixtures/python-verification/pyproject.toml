[project]
name = "controlled-development-python-fixture"
version = "1.0.0"
requires-python = ">=3.10"

[tool.controlled-development]
test-command = "python -m unittest discover -s tests"
type-check-command = "mypy src"
